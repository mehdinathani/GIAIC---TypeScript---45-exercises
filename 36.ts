function make_shirt_basic(size: string, message: string): void {
    console.log(`You've ordered a ${size} shirt with the following message: "${message}".`);
}

// Call the function
make_shirt_basic("medium", "Hello, world!");


